# prd-to-speckit-handoff

Convert approved PRD, acceptance criteria, ADRs, and traceability into Spec Kit-ready feature packets. Do not reopen product strategy.

## Rules

- Implicit invocation is disabled.
- Use only when a recipe explicitly lists `$prd-to-speckit-handoff`.
- Record outputs in the configured pipeline files.
