# Codex Adapter

Codex uses user-level skills in `$HOME/.agents/skills` and project skills in `.agents/skills`. Install cross-platform with `python3 scripts/install.py skills --target codex` and `python3 scripts/install.py scaffold /path/to/repo` (use `python` on Windows).
