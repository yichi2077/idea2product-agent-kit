# Codex Adapter

Codex uses user-level skills in `$HOME/.agents/skills` and project skills in `.agents/skills`. Install user skills with `scripts/install_user_skills.ps1`; scaffold project skills with `scripts/scaffold_into_repo.ps1`.
