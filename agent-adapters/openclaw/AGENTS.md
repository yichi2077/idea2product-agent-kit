# idea2product for OpenClaw

Use `agent-adapters/common/IDEA2PRODUCT_AGENT_GUIDE.md` as the workspace operating instructions. OpenClaw can load the `idea2product-*` skills from this workspace's `skills/` directory, `.agents/skills`, `~/.agents/skills`, or `~/.openclaw/skills`.
