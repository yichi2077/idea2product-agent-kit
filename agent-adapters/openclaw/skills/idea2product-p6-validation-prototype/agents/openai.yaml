interface:
  display_name: "idea2product-P6-validation-prototype"
  short_description: "Validate the single core interaction with a throwaway prototype tested on ~5 target users before architecture/build; the conversations are the evidence."
  default_prompt: "Use $idea2product-p6-validation-prototype in the current workspace."
policy:
  allow_implicit_invocation: false
