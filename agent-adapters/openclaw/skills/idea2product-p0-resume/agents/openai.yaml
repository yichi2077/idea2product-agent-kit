interface:
  display_name: "idea2product-P0-resume"
  short_description: "Resume the idea-to-product pipeline from saved state by reporting current status, identifying the next ready phase or blocker, and continuing only when the user asked to proceed."
  default_prompt: "Use $idea2product-p0-resume to continue the pipeline."
policy:
  allow_implicit_invocation: false
