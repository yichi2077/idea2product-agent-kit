# OpenClaw Tool Notes

Use shell access for `python3 .pipeline/scripts/pipeline.py ...` on macOS/Linux or `python .pipeline/scripts/pipeline.py ...` on Windows. Never approve a gate autonomously; in light mode (default) record the user's explicit in-chat approval, in strict mode the user approves in a real terminal (see the operating guide).
