interface:
  display_name: "idea2product-P0-doctor"
  short_description: "Run the read-only idea-to-product pipeline health check and report missing, stale, scaffold, or inconsistent state."
  default_prompt: "Use $idea2product-p0-doctor to health-check the current workspace."
policy:
  allow_implicit_invocation: false
