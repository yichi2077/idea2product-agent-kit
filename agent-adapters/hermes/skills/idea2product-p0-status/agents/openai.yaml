interface:
  display_name: "idea2product-P0-status"
  short_description: "Report the current state of the idea-to-product pipeline, including active phase, mode, pilot validation state, gate states, and blockers, without changing pipeline files."
  default_prompt: "Use $idea2product-p0-status in the current workspace."
policy:
  allow_implicit_invocation: false
