# Installation Report

- Spec Kit installed from `github/spec-kit` tag `v0.10.2` with `uv tool install`; verified as `specify 0.10.2`.
- Microsoft Playwright CLI installed with npm; `playwright-cli --version` returns `0.1.14`.
- Microsoft Playwright package installed to expose the common `playwright` command; `playwright --version` returns `Version 1.60.0`.
- Context7 CLI installed with npm as `ctx7@0.5.2`; verified as `0.5.2`.
- `gh` CLI is not installed on this machine.
- No third-party install scripts from downloaded repositories were run.
- No MCP servers were enabled by default.
