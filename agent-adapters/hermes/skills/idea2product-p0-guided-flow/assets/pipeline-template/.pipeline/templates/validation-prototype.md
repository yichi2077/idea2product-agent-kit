# Validation Prototype — <name>

<!-- P6 (new): the prototype is NOT evidence — the user conversations are. Build the ONE
     core interaction, put it in front of ~5 target-persona users, observe behavior.
     WAIVABLE: if you deliberately skip, set Decision: skip_with_rationale and log the risk. -->

## Single core interaction
<!-- The one indispensable interaction: if a user could try only ONE thing to judge value,
     what is it? Everything else is cut for this prototype. -->

## What we built (throwaway scope)
<!-- The smallest version of that interaction. State explicitly what was faked / stubbed / cut. -->

## Users tested (target persona, ~5)
| User | Fits validated target persona? | How recruited |
| --- | --- | --- |

## Observed behavior (not stated preference)
<!-- What they DID: where they hesitated, succeeded, or gave up. Verbatim quotes. -->

## Evidence vs the hypothesis
<!-- What this confirmed / challenged / surprised. Does the validated problem match the one
     assumed at P1? Confirm/challenge asymmetry — did you actively seek disconfirming signal? -->

## Early PMF signals (from the ~5 users)
<!-- Would they be "very disappointed" without it, or just polite? Are they pulling for more
     (asking when they can use it) or do you have to push? Any early Retention / Revenue /
     Referral intent? These inform go/pivot — they are signal, not statistical proof. -->

## If stuck (pivot diagnostics)
<!-- If a few iterations show no meaningful progress: (1) is a sub-segment reacting differently
     (look at sub-groups, not the aggregate)? (2) is the gap a messaging problem or a product
     problem? (3) what must be true for this to reach PMF — and is that realistic? If not, pivot. -->

## Decision
[go | pivot | back_to_discovery | skip_with_rationale]

## Rationale
<!-- Grounded in the conversations and the registers. If skip_with_rationale: the explicit
     reason and the risk you are accepting (also recorded in the risk register). -->

## Open Questions
