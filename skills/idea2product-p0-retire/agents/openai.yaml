interface:
  display_name: "idea2product-P0-retire"
  short_description: "Retire the current idea-to-product project after explicit confirmation and a recorded reason."
  default_prompt: "Use $idea2product-p0-retire to retire the current pipeline."
policy:
  allow_implicit_invocation: false
