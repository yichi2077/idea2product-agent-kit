interface:
  display_name: "idea2product-P3-strategy-decision"
  short_description: "Convert strategy analysis into a decision memo comparing build, buy, partner, capability-only, internal improvement, wait, and do-nothing options, then red-team the recommendation and request Strategy Gate."
  default_prompt: "Use $idea2product-p3-strategy-decision in the current workspace."
policy:
  allow_implicit_invocation: false
